# Pipeline Status

Operational handoff only. `LEADS.md` and `OUTREACH_LOG.md` remain the source of truth.

- Current phase: Build complete, QA passed. Not yet deployed or sent.
- Last trusted commit: (see `git log -1` after this build's commit)
- Known untrusted state: none — build committed locally with QA verdict PASS.
- Next exact action: Deploy phase — new public GitHub repo `wild-nature-studio-demo` under Plainset, push, enable Pages, confirm live URL, then draft outreach email per AGENTS.md step 7 (lead with the SSL/HTTPS handshake failure as the verified observation hook — see BUILD_BRIEF.md Pitch Hook).
- Deploy URL: not yet deployed (build phase only, per task scope).
- Outreach state: not yet drafted or sent. Note: the studio's real domains are HTTPS-broken (see BUILD_BRIEF.md), so any outreach email should NOT link to wildnaturestudio.com over https:// when referencing their current site — use http:// or just describe the issue in prose.
- Flags for Alex:
  - Could not independently re-verify the Facebook "4.0★/4 reviews" figure logged in LEADS.md this session (Facebook blocked to plain curl, browser tool denied). Not used in site copy; only the Facebook page link itself (confirmed genuine via the business's own site) was used.
  - Only a partial address is publicly available (RG1, Reading, Berkshire) — no full street address found on the business's own site or its Wayback snapshot.
  - Phone number +44 792 772 8919 is newly found this session (was blank in LEADS.md's Contact column) — worth back-filling into LEADS.md.
